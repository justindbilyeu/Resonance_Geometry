name: dissertation-build (archived)

concurrency:
  group: paper-builds
  cancel-in-progress: true

# ⚠️ October 2025: Paper builds paused during analysis integration.
# Runs manually via GitHub Actions → "Run workflow".
on:
  workflow_dispatch:
    inputs:
      build_target:
        description: "Which document(s) to compile"
        required: false
        default: "dissertation"

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::warning::The dissertation PDF pipeline is archived/paused."
          echo "Markdown + figures + data remain the canonical sources."
          echo "To re-enable, restore push triggers and the original build steps."
