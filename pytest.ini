[pytest]
addopts = -ra
testpaths = tests
xfail_strict = true
markers =
    slow: takes more than a couple of seconds (simulation sweeps)
filterwarnings =
    error::DeprecationWarning
    error::FutureWarning
