"""Smoke tests for the simulations package.

These tests exist to keep continuous integration invocations such as
``pytest simulations`` from failing with "no tests collected".
"""

def test_placeholder():
    """A minimal assertion so pytest reports a passing test run."""
    assert True
